import { StyleSheet } from "react-native";
import GlobalVars from "../../../global/globalVars"
const styles = StyleSheet.create({
  calendar: {
    width:GlobalVars.windowWidth/1.2
  },

});

export default styles;
