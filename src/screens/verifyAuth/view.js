import React from "react";
import { View, Text } from "react-native";

const Element = (props) => {
  const { auth } = props;

  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Element;
